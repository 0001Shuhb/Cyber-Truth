version: '3.8'
services:
  backend:
    build:
      context: ..\backend
      dockerfile: ../docker/Dockerfile.backend
    ports:
      - "5000:5000"
  frontend:
    build:
      context: ..\frontend
      dockerfile: ../docker/Dockerfile.frontend
    ports:
      - "5173:5173"
